# PawID 纯前端交互演示 PRD：

版本：V1.0｜用途：直接交给 AI 开发可操作的前端演示网站。

依据：[用户提供的产品方案](https://chatgpt.com/share/6a9b8c71-e368-83e8-9578-32aed40ed51f)。保留宠物数字身份、AI 形象、人宠绑定、共同监护、族谱、成长资产、防丢和服务生态；下述路由、样式、数值、示例人物及状态规则为本次演示的具体设计。

## 1. 产品目标与边界：

**产品名称：PawID · 宠物数字生命护照。**

一句话：围绕同一只宠物，连接它的数字形象、家庭关系、生命记录、成长资产与现实安全。

演示目标：在约 5 分钟内完成“创建身份 → 查看数字形象 → 添加家人和父母 → 获得积分并换装 → 发布寻宠并模拟寻回”的完整体验。

- 全站中文，保留 PawID、PAWS 等品牌词；优先适配电脑演示，同时支持手机浏览。
- 仅开发前端；使用本地预置数据、模拟异步请求及浏览器存储，无需后端、API Key、真实钱包或链上交易。
- 所有页面固定显示小型“演示模式”标识；模拟 AI 结果、机构凭证和身份交易在各自详情中明确标注。
- 本期必须完成第 2 节全部页面及第 3 节交互。医院、保险、领养、品牌合作、纪念服务以可查看的服务卡片展示，不开发业务后台、真实支付和真实通知。
- 禁止出现无响应按钮、空白详情页、假外链；未实现的未来能力仅在服务详情中说明。

## 2. 页面结构与层级：

采用“品牌入口 + 宠物工作台 + 公开访问页面”。宠物工作台通过顶部宠物切换器维持当前宠物，切换标签不丢失宠物 ID。

| 层级 | 页面 / 路由 | 布局与板块顺序 | 核心入口与去向 |
|---|---|---|---|
| 一级 | 首页 `/` | 顶部品牌导航；左文案右宠物数字卡的首屏；身份/家庭/成长/安全四张价值卡；演示流程说明 | 创建 PawID → `/create`；体验示例 → `/pets/mochi?tab=overview`；服务生态 → `/ecosystem` |
| 一级 | 创建身份 `/create` | 顶部四步进度；左侧操作区约 60%，右侧身份卡预览约 40%；底部上一步/下一步 | 完成后 → `/pets/:id?tab=overview` |
| 一级 | 宠物工作台 `/pets/:id` | 顶栏；左侧 220px 导航；右侧内容。导航含总览、家庭族谱、生命档案、成长资产、防丢守护；底部服务生态入口 | 五个标签使用 `?tab=overview/family/life/assets/safety`；默认总览 |
| 二级 | 总览 `tab=overview` | 左侧大尺寸数字形象；右侧身份资料及监护人；下方四项统计、快捷操作、最近三条动态 | 邀请家人 → 家庭；记录成长 → 生命；兑换装扮 → 资产；防丢设置 → 守护 |
| 二级 | 家庭族谱 `tab=family` | 顶部“监护家庭/宠物族谱”切换；主体关系卡或图谱；右侧节点详情抽屉 | 邀请监护人、添加父母、接受/拒绝申请、查看节点资料 |
| 二级 | 生命档案 `tab=life` | 顶部事件筛选和新增按钮；左侧时间线约 65%；右侧凭证列表约 35% | 新增记录弹窗；事件详情弹窗；凭证详情抽屉 |
| 二级 | 成长资产 `tab=assets` | 宠物账户与余额；成长任务；“徽章/衣橱/积分明细”切换；下方装扮兑换区 | 查看任务奖励、兑换、穿戴/卸下、查看模拟交易 |
| 二级 | 防丢守护 `tab=safety` | 左侧 PawTag 二维码卡；右侧安全状态与设置；下方寻宠信息、线索列表 | 标记走失；打开访客预览 → `/tag/:id`；确认已接回 |
| 一级公开 | 邀请确认 `/invite/:inviteId` | 居中邀请卡；邀请人、宠物及关系摘要；接受/拒绝；结果卡 | 接受后展示共同监护彩蛋；返回该宠物的家庭页 |
| 一级公开 | PawTag 访客页 `/tag/:id` | 手机优先单列；宠物照片与姓名；安全/走失状态；公开特征；帮助联系表单 | “我发现了它”→ 提交线索；成功后展示反馈状态 |
| 一级 | 服务生态 `/ecosystem` | 顶部价值说明；“宠主服务/机构服务/公益服务”分类；服务卡片网格 | 点击卡片 → 详情弹窗；选择感兴趣 → 本地记录意向 |

补充：不再单独创建消息中心、个人中心、后台管理。消息入口用顶栏抽屉，展示邀请结果、积分奖励和寻宠线索，并跳转对应宠物及标签。

## 3. 页面功能与交互规则：

### 3.1 创建身份：

| 步骤 | 内容与输入 | 交互、校验与结果 |
|---|---|---|
| ① 上传照片 | 拖拽/选择 JPG、PNG、WebP；单张不超过 5MB；提供“使用示例猫咪/狗狗” | 选择后显示预览，允许替换；类型或大小不符时原位提示；未选择照片不可下一步 |
| ② 确认信息 | 宠物名必填 1–20 字；猫/狗必选；品种选填；性别含未知；生日可未知；毛色可编辑 | 示例宠物预填资料；任意上传只给待确认示例值，不声称真实识别；生日不可晚于今天 |
| ③ 选择数字形象 | 3D 萌宠、像素宠物、插画宠物三种风格；原图与数字形象并列 | 点击生成，依次展示“分析特征/生成形象/完成”，约 2 秒；返回预置资产并标注“预置形象演示”；换风格重新预览 |
| ④ 创建 PawID | 姓名、形象、生日等确认；当前演示用户作为主监护人；公开字段预览 | 点击创建：确认中 → 身份生成中 → 成功，约 2 秒；成功时统一生成宠物 ID、模拟账户、监护关系、出生/创建事件，并奖励 100 PAWS |

规则：上一步保留输入；处理中禁用重复提交；创建成功只执行一次，展示身份卡及轻量庆祝动效，然后点击“进入它的数字世界”打开总览。创建草稿在刷新后恢复，退出创建时可选择保留或清除。

### 3.2 宠物总览：

- 身份卡：真实照片缩略图、数字形象、姓名、物种/品种、年龄或“生日未知”、PawID、监护人、陪伴天数、状态。
- 四项统计：家庭成员数、成长记录数、徽章数、PAWS 余额，均读取当前宠物实际演示数据。
- “身份凭证（模拟）”打开抽屉：身份编号、创建时间、演示网络 Monad、模拟账户、模拟交易 ID；可复制，不跳真实区块浏览器。
- “编辑资料”弹窗修改姓名、性别、生日、介绍；保存后身份卡、访客页及图谱名称同步更新，宠物 ID 不变。
- 数字形象有轻微呼吸/悬浮效果；穿戴的装扮覆盖在形象上并同步展示。

### 3.3 家庭与族谱：

**监护家庭：**主监护人卡置顶，共同监护人并排，下方为待处理邀请。

- 点击“邀请家人” → 填写昵称（1–20 字） → 生成邀请链接及二维码 → 可复制链接或“模拟受邀者打开”。默认关系为共同监护，不推断情侣或婚姻关系。
- 受邀者确认页明确当前模拟身份；接受后，家庭页增加一名共同监护人，记录加入时间，同时展示“共同守护者解锁 / 一起陪伴它成长”的双人宠物卡和彩带效果。
- 每只宠物首次新增共同监护人奖励 50 PAWS；重复点击、重复打开已接受邀请均不重复添加或发奖。拒绝后原邀请不可再接受，可由主监护人重新创建。
- 受邀者不能是主监护人或已有成员；邀请记录明确关联宠物 ID。

**宠物族谱：**默认展示以当前宠物为中心的三代局部图谱；父母在上方、祖父母再上一层、兄弟姐妹与当前宠物同层。

- 支持拖拽画布、缩放、回到中心；缺少父母显示“添加父亲/母亲”虚线节点，不虚构未知亲缘。
- 点击添加 → 按名称或 PawID 搜索预置宠物 → 选择对象 → 提交申请 → 显示待确认虚线 → “模拟对方处理”接受/拒绝。
- 接受后连线变实线，标记“双向确认”，相关宠物视角同步更新；首次建立父母关系奖励 50 PAWS。
- 禁止绑定自身、重复父母槽位、跨物种亲缘、同一对象同时作为父母，或形成祖先循环；父母已知生日必须早于子女；性别未知可选择父母角色，已知性别与角色冲突时提示。
- 兄弟姐妹由已确认的共同父母关系派生；节点点击显示资料抽屉，其中“查看档案”打开对应宠物总览。无监护权限的档案使用只读模式。
- “用户填写”“双方确认”“机构认证（示例）”使用不同标签；双方确认不等同于血统真实性认证。

### 3.4 生命档案：

- 时间线默认倒序；筛选项：全部、家庭、健康、生日、旅行、日常。
- “新增记录”弹窗字段：类型、标题（必填，1–30 字）、日期（必填且不晚于今天）、描述（选填，最多 500 字）、照片（选填，同上传限制）、可见性（默认仅家庭）。
- 保存后时间线插入新记录，总览动态和记录数同步变化；首次手动记录奖励 20 PAWS；自动创建事件不触发该奖励。
- 手动记录可编辑、删除，删除需确认；积分不重复发放，删除也不重置首次奖励资格。身份创建、成员加入等系统事件只读。
- 凭证卡展示疫苗、身份、血统等预置例子；详情含签发者、签发时间、状态、演示摘要。手动健康记录只标记“用户填写”，不可自动变成机构认证。
- 私密描述、健康详情及家庭成员信息不进入 PawTag 公开视图。

### 3.5 成长资产：

- 页面标题为“属于 Mochi 的成长资产”，以当前宠物为资产主体；展示 PAWS、徽章、已拥有装扮及演示账户。
- 任务列表实时展示“未完成/已完成”，奖励随业务成功自动到账，不额外要求重复领取；可提供“去完成”跳转对应功能。
- 固定奖励：创建身份 +100；首次共同监护 +50；首次确认父母 +50；首次手动成长记录 +20。首次确认接回发放 Home Again 徽章，无积分。
- 装扮兑换固定三件：蝴蝶结 60、生日帽 120、星空披风 200 PAWS。点击兑换 → 预览及余额变化 → 确认 → 扣分、入衣橱、写入明细。
- 余额不足提示差额并提供“查看成长任务”；已拥有商品显示“已拥有”，不可重复扣费。
- 衣橱支持穿戴/卸下；本期同一时刻最多穿戴一件，换装同步总览。徽章点击查看获得原因及时间。
- 明细显示时间、事项、增减积分、变更后余额。积分仅用于演示，不支持充值、提现或交易。

### 3.6 防丢与寻回：

- 守护页展示基于当前访问地址生成的 PawTag 二维码、复制链接、打开访客预览。
- 正常状态显示绿色“安全在家”；点击“标记走失”填写最后出现地点（必填）、时间（必填，不晚于当前时间）、补充描述后确认，状态变为红色“寻找中”。
- PawTag 访客无需登录。只显示宠物公开照片、姓名、物种、外观特征和走失说明，不显示主人的手机号、住址、账户及健康档案。
- 访客点击“我发现了它”填写发现地点（必填）、留言（选填）、回访联系方式（选填），提交后显示“线索已记录（演示）”。位置采用手动填写，不触发真实定位权限。
- 同浏览器的主人页收到本地线索通知，状态变为橙色“收到线索”；收到线索不等于已寻回。主人在查看线索后点击“确认已接回”，状态恢复安全，新增生命事件并首次发放 Home Again 徽章。
- 安全状态下被扫码可提交线索，但不自动把宠物变为走失。恢复安全后历史线索保留；再次走失生成新的寻宠事件 ID，旧线索不改变本次状态。
- 主人可“结束本次寻宠”，二次确认后恢复安全，但不发寻回徽章。

**纯前端同步边界：**同源、同浏览器多标签页通过存储事件同步；不同手机没有共享本地数据，不能真实通知主人。预置宠物的二维码可在另一设备展示预置资料；自建宠物和邀请在另一设备无法解析时展示“此内容仅在创建它的演示浏览器可用”并提供“体验示例”。现场互动统一使用“打开访客预览/模拟受邀者打开”完成，不承诺跨设备实时同步。

### 3.7 服务生态：

| 分类 | 必备卡片 | 详情必须表达的价值 |
|---|---|---|
| 宠主服务 | PawID Plus、AI 数字内容、PawTag 宠物牌 | 订阅权益、宠物形象与纪念内容、数字身份连接现实防丢；价格统一“方案待定” |
| 机构服务 | 医院健康凭证、猫舍犬舍来源证明、保险授权资料、品牌活动 | 连续档案、信息来源核验、按宠主授权查看、品牌会员与活动权益 |
| 公益服务 | 救助领养、寻回协作、生命纪念 | 监护变更后身份与历史连续、协助寻找宠物、保留生命记忆 |

每张卡展示服务对象、解决的问题及未来收费方式或公益属性；弹窗中提供“感兴趣/取消意向”按钮并本地保存。此处仅表达产品与商业价值，不生成订单、不展示虚构合作机构或真实认证。

## 4. 关键跳转链路：

| 演示场景 | 完整链路 | 验证结果 |
|---|---|---|
| 新建数字身份 | 首页 → 创建四步 → 成功卡 → 总览 → 身份凭证抽屉 | 宠物资料、主监护关系及 100 PAWS 一致 |
| 共同养宠彩蛋 | 总览 → 家庭 → 邀请弹窗 → 模拟受邀者页面 → 接受 → 彩蛋卡 → 家庭页 | 新成员、加入记录、50 PAWS 同步，不能重复奖励 |
| 族谱建立 | 家庭 → 族谱 → 添加父母 → 搜索选择 → 待确认 → 模拟接受 | 图谱从虚线变实线，节点可查看，相关档案一致 |
| 成长与消费 | 总览 → 生命档案 → 新增记录 → 成长资产 → 兑换 → 穿戴 → 总览 | 记录增加、积分到账与扣除准确、形象更新 |
| 防丢价值 | 防丢守护 → 标记走失 → 访客预览 → 提交线索 → 主人查看 → 确认接回 | 状态正确流转，新增寻回记录与徽章 |
| 生态价值 | 首页或侧栏 → 服务生态 → 切换分类 → 查看服务 → 记录意向 | 每项服务有明确用户价值，意向刷新后保留 |

## 5. 视觉、布局与效果：

| 项目 | 明确规范 |
|---|---|
| 整体方向 | 温暖、轻科技、宠物生活感；浅色背景、紫色重点、较大宠物形象，信息层级清楚 |
| 色彩 | 主色 `#7765F5`；背景 `#F7F7FC`；卡片 `#FFFFFF`；正文 `#202333`；次级文字 `#697386`；成功 `#16A085`；待处理 `#D99000`；走失 `#D64550` |
| 字体 | 系统中文无衬线；首页主标题 44px，页面标题 28px，模块标题 18px，正文 14–16px，辅助文字 12px；正文行高 1.6 |
| 桌面布局 | 以 1440px 设计；顶栏高 64px，侧栏宽 220px；内容区最大 1200px；外边距 32px；模块间距 24px；卡片内边距 20px |
| 手机布局 | 小于 768px 改为单列；工作台五项导航改底部栏，高 64px 并留安全区；服务生态放顶栏菜单；创建页预览折叠；图谱保留缩放和拖动 |
| 组件 | 卡片圆角 16px，按钮圆角 10px且高 44px；表单标签位于输入框上方；主操作紫底白字，次操作描边；状态用文字+颜色共同表达 |
| 图像 | 为每个预置宠物准备真实照片与三种数字形象；任意上传使用对应猫/狗示例形象并标注；资源随项目提供，不能依赖会失效的外链 |
| 交互效果 | hover 卡片上浮 2px；抽屉/弹窗 200ms；形象轻微呼吸；创建和共同监护成功时一次性彩带；支持减少动态效果偏好 |
| 反馈与弹层 | 成功 Toast 约 3 秒；错误提示保留至纠正；抽屉桌面宽 420px、手机全屏；弹窗支持关闭及 Esc，关闭未保存表单先提示 |
| 可操作性 | 按钮支持键盘，焦点清楚；图标按钮带文字提示；复制失败展示可手动复制文本；空列表含解释和明确下一步 |

## 6. 数据、权限与演示控制：

- 建议采用 React + TypeScript + Vite；路由使用 Hash 模式，文档中的 `/pets/...` 对应实际 `/#/pets/...`，适合纯静态部署及刷新直达。无需服务端运行时。
- 核心数据对象：用户 User、宠物 Pet、监护关系 GuardianLink、邀请 Invite、亲缘关系 Relation、生命事件 LifeEvent、凭证 Credential、积分明细 PointEntry、装扮 InventoryItem、寻宠事件 LostCase、线索 FoundReport。
- 所有记录以 `petId` 关联；监护关系单独记录 `userId` 与角色；积分余额由初始数据及明细计算，发奖使用“宠物 ID + 任务类型”唯一标识防重。
- 结构化数据存 localStorage，上传图片存 IndexedDB；生成模拟账户和交易 ID 后保持稳定。图片存储失败时保留表单并提示改用示例图。
- 预置用户：小林（主监护人）、阿宁（受邀者）；主宠 Mochi；父亲 Leo、母亲 Coco、同父母姐妹 Luna，以及可展开查看的四个祖辈。Mochi 的父亲和共同监护关系初始为空，便于现场新增；其余数据保持一致；预置亲缘和历史记录不触发本次交互任务奖励，首次奖励仅统计进入演示后的成功操作。
- 示例初始余额 100 PAWS、6 条生命记录、2 张明确标注的示例凭证、1 枚身份徽章、0 件装扮。用户新建宠物只拥有创建产生的数据，不套用示例历史。
- 编辑、兑换、邀请、标记走失等操作仅对有监护权限的演示身份开放；主监护人才可邀请家人及提交亲缘申请。公开页与无监护关系的档案只读。前端权限仅作体验模拟，不代表真实安全隔离。
- 顶栏提供“演示控制”：当前角色、切换主人/受邀者/访客、开启下次操作失败、重置演示。所有辅助操作收纳于此，不混入日常主流程。
- “下次操作失败”作用于下一次模拟异步提交：失败不写入数据、不扣分，保留输入并允许重试。成功提交一次性更新关联数据，重复点击不重复生效。
- “重置演示”需二次确认，仅清除本项目命名空间下的存储及上传图片，恢复初始种子数据，不调用全站存储清空。
- 无效宠物 ID、邀请 ID、路由均展示解释页和“回到首页/体验示例”；待确认、已接受、已拒绝邀请有独立显示状态；刷新后恢复当前页面和数据。

## 7. 开发交付与验收：

开发 AI 必须交付完整可运行源码、本地图片资源、启动说明和静态构建产物；说明文档写清安装、启动、构建命令与 5 分钟演示步骤。不得要求申请 API 或安装钱包。

验收标准：

1. 全部指定路由和五个工作台标签可访问，浏览器前进/后退与刷新正常。
2. 第 4 节六条链路均可完整操作，不使用仅弹“成功”却不更新数据的占位实现。
3. 姓名、关系、积分、装扮、记录数与安全状态跨页面一致；刷新后仍保留。
4. 重复接受邀请、重复提交、重复领奖、余额不足、取消兑换均不会产生错误数据。
5. 创建过程、邀请和族谱待确认、空记录、模拟失败、已拒绝邀请均有清楚界面及恢复路径。
6. PawTag 仅暴露允许公开的数据；收到线索后必须由主人确认接回；不同设备的演示限制清楚可见。
7. 桌面 1440px 与手机 390px 下无意外横向溢出，图谱可在自己的画布内平移，底部导航不遮挡按钮。
8. AI 形象、模拟身份交易、示例机构凭证均明确标注；不暗示真实 AI 已生成、真实交易已确认或真实机构已认证。

**给开发 AI 的执行要求：按本 PRD 一次性完成页面、状态、数据联动及演示链路；直接使用规定的默认值，不自行扩展后台、支付、社交信息流或复杂 Web3 功能。优先保证可演示、可理解、可连续操作。**
