# PawIDRegistry 合约部署变量（仅名称与无敏感说明；本文件可提交，真实值绝不入库）
# Monad Testnet chainId 冻结为 10143，禁止改为其他链。

# 测试网 RPC（默认 https://testnet-rpc.monad.xyz，可按需覆盖）
# MONAD_TESTNET_RPC_URL=https://testnet-rpc.monad.xyz

# 已部署/待检查的注册表地址（部署前保持为空；脚本缺失即失败，禁止臆测地址）
# REGISTRY_ADDRESS=

# 本地 Foundry 回退部署签名（仅本地安全环境导出使用；绝不提交、绝不回显、绝不写入仓库）
# DEPLOYER_PRIVATE_KEY=

# 验证载荷输入（forge verify-contract --show-standard-json-input 的输出文件路径）
# STANDARD_JSON_PATH=
